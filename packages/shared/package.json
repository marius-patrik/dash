{
  "name": "@dash/shared",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "echo 'shared: watching not needed, used via workspace link'"
  }
}
